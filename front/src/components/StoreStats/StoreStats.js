// @flow

import React from "react";

import "./StoreStats.scss";

type PropsType = {
  //
};

const StoreStats = (props: PropsType) => (
  <div className="StoreStats widget">StoreStats</div>
);

export default StoreStats;
